<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="dieuxe.Views.xemlichdadangky">
    <ContentPage.Content>
        <StackLayout>
            <!--<SearchBar TextChanged="SearchBar_TextChanged"></SearchBar>-->
            <ListView ItemTapped="sualich_ItemTapped"  IsPullToRefreshEnabled="True" x:Name="sualich" ItemsSource="{Binding listLich}" HasUnevenRows="True">

                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <StackLayout Orientation="Vertical" Padding="12,6">
                                <Label Text="{Binding ngaydangky}" FontSize="24"/>
                                <Label Text="{Binding noidi}" FontSize="24" Opacity="0.6"/>
                                <Label Text="{Binding noiden}" />
                            </StackLayout>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>